services:
  flask:
    build:
      context: .
      dockerfile: flask_server/Dockerfile
    volumes:
      - ./flask_server:/app
      # Dependencies are managed with uv inside the container
    environment:
      - FLASK_ENV=development
      - FLASK_APP=src.app:app
    ports:
      - "${FLASK_PORT:-5001}:5001"

  react:
    image: node:18
    working_dir: /app
    volumes:
      - ./client:/app:cached
      - /app/node_modules
    command: sh -c "npm install --silent && npm start"
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true
      - REACT_APP_API_URL=http://flask:5001
    depends_on:
      - flask
